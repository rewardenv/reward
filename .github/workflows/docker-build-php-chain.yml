# yaml-language-server: $schema=https://json.schemastore.org/github-workflow
---
name: "PHP Chain • Docker Build"
on:
  schedule:
    - cron: "0 6 1 * *"
  workflow_dispatch:
  push:
    paths:
      - images/.trigger
      - images/php/.trigger
    branches:
      - main
      - feature/revamp-php-build
jobs:
  php-cli:
    runs-on: ubuntu-latest
    steps:
      - name: "PHP/CLI • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP/CLI • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}

  php-cli-loaders:
    runs-on: ubuntu-latest
    needs: [php-cli]
    steps:
      - name: "PHP/CLI-Loaders • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP/CLI-Loaders • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}

  php-fpm:
    runs-on: ubuntu-latest
    needs: [php-cli]
    steps:
      - name: "PHP/FPM • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP/FPM • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}

  php-fpm-loaders:
    runs-on: ubuntu-latest
    needs: [php-fpm]
    steps:
      - name: "PHP/FPM-Loaders • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP/FPM-Loaders • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}

  php-fpm-base:
    runs-on: ubuntu-latest
    needs: [php-fpm-loaders]
    steps:
      - name: "PHP-FPM Base • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP-FPM Base • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}

  php-fpm-blackfire:
    runs-on: ubuntu-latest
    needs: [php-fpm-base]
    steps:
      - name: "PHP-FPM Blackfire • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP-FPM Blackfire • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}

  php-fpm-magento1:
    runs-on: ubuntu-latest
    needs: [php-fpm-base]
    steps:
      - name: "PHP-FPM Magento 1 • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP-FPM Magento 1 • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}

  php-fpm-magento2:
    runs-on: ubuntu-latest
    needs: [php-fpm-base]
    steps:
      - name: "PHP-FPM Magento 2 • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP-FPM Magento 2 • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}

  php-fpm-shopware:
    runs-on: ubuntu-latest
    needs: [php-fpm-base]
    steps:
      - name: "PHP-FPM Shopware • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP-FPM Shopware • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}

  php-fpm-wordpress:
    runs-on: ubuntu-latest
    needs: [php-fpm-base]
    steps:
      - name: "PHP-FPM WordPress • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP-FPM WordPress • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}

  php-fpm-spx:
    runs-on: ubuntu-latest
    needs: [php-fpm-base]
    steps:
      - name: "PHP-FPM SPX • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP-FPM SPX • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}

  php-fpm-xdebug2:
    runs-on: ubuntu-latest
    needs: [php-fpm-base]
    steps:
      - name: "PHP-FPM Xdebug 2 • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP-FPM Xdebug 2 • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}

  php-fpm-xdebug3:
    runs-on: ubuntu-latest
    needs: [php-fpm-base]
    steps:
      - name: "PHP-FPM Xdebug 3 • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP-FPM Xdebug 3 • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}

  php-fpm-magento2-web:
    runs-on: ubuntu-latest
    needs: [php-fpm-magento2]
    steps:
      - name: "PHP-FPM Magento 2 Web • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP-FPM Magento 2 Web • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}

  php-fpm-shopware-web:
    runs-on: ubuntu-latest
    needs: [php-fpm-shopware]
    steps:
      - name: "PHP-FPM Shopware Web • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP-FPM Shopware Web • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}

  php-fpm-wordpress-web:
    runs-on: ubuntu-latest
    needs: [php-fpm-wordpress]
    steps:
      - name: "PHP-FPM Shopware Web • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP-FPM Shopware Web • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}

  php-fpm-rootless-base:
    runs-on: ubuntu-latest
    needs: [php-fpm-loaders]
    steps:
      - name: "PHP-FPM Rootless Base • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP-FPM Rootless Base • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}

  php-fpm-rootless-blackfire:
    runs-on: ubuntu-latest
    needs: [php-fpm-rootless-base]
    steps:
      - name: "PHP-FPM Rootless Blackfire • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP-FPM Rootless Blackfire • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}

  php-fpm-rootless-magento1:
    runs-on: ubuntu-latest
    needs: [php-fpm-rootless-base]
    steps:
      - name: "PHP-FPM Rootless Magento 1 • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP-FPM Rootless Magento 1 • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}

  php-fpm-rootless-magento2:
    runs-on: ubuntu-latest
    needs: [php-fpm-rootless-base]
    steps:
      - name: "PHP-FPM Rootless Magento 2 • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP-FPM Rootless Magento 2 • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}

  php-fpm-rootless-shopware:
    runs-on: ubuntu-latest
    needs: [php-fpm-rootless-base]
    steps:
      - name: "PHP-FPM Rootless Shopware • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP-FPM Rootless Shopware • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}

  php-fpm-rootless-wordpress:
    runs-on: ubuntu-latest
    needs: [php-fpm-rootless-base]
    steps:
      - name: "PHP-FPM Rootless WordPress • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP-FPM Rootless WordPress • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}

  php-fpm-rootless-spx:
    runs-on: ubuntu-latest
    needs: [php-fpm-rootless-base]
    steps:
      - name: "PHP-FPM Rootless SPX • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP-FPM Rootless SPX • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}

  php-fpm-rootless-xdebug2:
    runs-on: ubuntu-latest
    needs: [php-fpm-rootless-base]
    steps:
      - name: "PHP-FPM Rootless Xdebug 2 • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP-FPM Rootless Xdebug 2 • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}

  php-fpm-rootless-xdebug3:
    runs-on: ubuntu-latest
    needs: [php-fpm-rootless-base]
    steps:
      - name: "PHP-FPM Rootless Xdebug 3 • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP-FPM Rootless Xdebug 3 • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}

  php-fpm-rootless-magento2-web:
    runs-on: ubuntu-latest
    needs: [php-fpm-rootless-magento2]
    steps:
      - name: "PHP-FPM Rootless Magento 2 Web • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP-FPM Rootless Magento 2 Web • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}

  php-fpm-rootless-shopware-web:
    runs-on: ubuntu-latest
    needs: [php-fpm-rootless-shopware]
    steps:
      - name: "PHP-FPM Rootless Shopware Web • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP-FPM Rootless Shopware Web • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}

  php-fpm-rootless-wordpress-web:
    runs-on: ubuntu-latest
    needs: [php-fpm-rootless-wordpress]
    steps:
      - name: "PHP-FPM Rootless WordPress Web • Docker Build"
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: "PHP-FPM Rootless WordPress Web • Docker Build"
          token: ${{ secrets.PERSONAL_TOKEN }}
